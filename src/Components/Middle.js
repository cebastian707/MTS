import React from "react";
import Machine from "./Machine";

const Middle = ({}) => {
    // Example initial values


    return (
        <div id="Machine">
            <div id="MachineTapeContainer">
                {/* Replace Tape with Machine and pass initial values and rules */}
                <Machine/>
            </div>
        </div>
    );
};

export default Middle;
